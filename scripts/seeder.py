# Seeds the database with initial data for testing and development purposes.
# Assumes that the database and tables have already been created.
# Assumes the app is not running when this script is executed.
# everything is created with the .env admin user
